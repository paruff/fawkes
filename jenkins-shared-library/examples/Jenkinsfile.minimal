/**
 * Minimal Jenkinsfile for Golden Path pipeline
 *
 * This is the simplest possible configuration using all defaults.
 */

@Library('fawkes-pipeline-library') _

goldenPathPipeline {
    appName = 'my-service'
    language = 'java'
}
