# .yamllint
extends: default

rules:
  line-length:
    max: 120  # or higher if needed
    level: warning
  document-start: disable
  comments-indentation: disable
  indentation:
    spaces: 2
    indent-sequences: consistent
  trailing-spaces: enable
  empty-lines:
    max: 2