apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin
  namespace: jenkins
  annotations:
    fawkes.io/managed-by: external-secrets
    fawkes.io/example: "true"
type: Opaque
stringData:
  ADMIN_PASSWORD: "CHANGEME_NOT_COMMITTED" # placeholder; managed via External Secrets Operator