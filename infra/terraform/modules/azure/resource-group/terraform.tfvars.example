# Example configuration for Azure Resource Group module (azure/resource-group)

# Required variables
name     = "fawkes-rg"
location = "eastus2"

# Tags
tags = {
  platform    = "fawkes"
  managed_by  = "terraform"
  environment = "dev"
}
