apiVersion: v1
kind: Service
metadata:
  name: ai-code-review
  namespace: fawkes
  labels:
    app: ai-code-review
    component: ai-platform
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: ai-code-review
