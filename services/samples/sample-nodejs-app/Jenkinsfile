/**
 * Jenkinsfile for sample-nodejs-app
 * Generated from Node.js Express template
 */

@Library('fawkes-pipeline-library') _

goldenPathPipeline {
    appName = 'sample-nodejs-app'
    language = 'node'

    // Node.js build commands
    buildCommand = 'npm ci'
    testCommand = 'npm test -- --ci --reporters=jest-junit'
    bddTestCommand = 'npm run test:bdd'

    // Docker registry configuration
    dockerRegistry = 'harbor.fawkes.local'

    // SonarQube configuration
    sonarProject = 'sample-nodejs-app'

    // Notification channel
    notifyChannel = 'ci-builds'
}
