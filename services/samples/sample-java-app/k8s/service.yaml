apiVersion: v1
kind: Service
metadata:
  name: sample-java-app
  namespace: fawkes-samples
  labels:
    app: sample-java-app
    team: platform-team
spec:
  type: ClusterIP
  selector:
    app: sample-java-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
