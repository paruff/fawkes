apiVersion: v1
kind: ConfigMap
metadata:
  name: space-metrics-config
  namespace: fawkes-local
  labels:
    app: space-metrics
    component: devex
data:
  aggregation-threshold: "5"
  collection-interval: "3600"
  retention-days: "90"
