# ============================================================================
# FILE: platform/apps/datahub/datahub-frontend-secret.yaml
# PURPOSE: Secret for DataHub frontend application
# SECURITY: Generate random secret for production deployment
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: datahub-frontend-secret
  namespace: fawkes
  labels:
    app: datahub
    component: frontend
    environment: dev
    team: platform
  annotations:
    description: "Frontend secret - generate random value for production"
type: Opaque
stringData:
  # Application secret used for session encryption
  # TODO: Generate a random secret for production using: openssl rand -base64 32
  secret: "YouKnowNothing-ChangeForProduction"
