# ============================================================================
# FILE: platform/apps/postgresql/db-vsm-credentials.yaml
# PURPOSE: PostgreSQL credentials for VSM database
# SECURITY: These are dev credentials. In production, use External Secrets Operator
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: db-vsm-credentials
  namespace: fawkes
  labels:
    app: vsm-service
    component: database
    environment: dev
type: Opaque
stringData:
  # Database credentials
  username: vsm_user
  password: vsm_password_changeme
  # Full connection string
  pgpass: "db-vsm-dev-rw.fawkes.svc:5432:vsm_db:vsm_user:vsm_password_changeme"
