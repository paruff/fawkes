# ============================================================================
# FILE: platform/apps/postgresql/db-harbor-credentials.yaml
# PURPOSE: Database credentials for Harbor container registry
# NOTE: For development/local use only. In production, use External Secrets
#       Operator (externalsecret-db-harbor.yaml) with Vault/AWS Secrets Manager.
# SECURITY: These credentials are for local development only. Generate secure
#           random passwords for any shared or production environments.
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: db-harbor-credentials
  namespace: fawkes
  labels:
    app: harbor
    component: database
    environment: dev
    team: platform
type: Opaque
stringData:
  username: harbor_user
  # Generate secure password in production: openssl rand -base64 32
  password: "dev_harbor_7n8K2mP9xQ4wR5tY"
  # PostgreSQL connection string for convenience
  dbname: harbor
