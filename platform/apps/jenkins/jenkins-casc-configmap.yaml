apiVersion: v1
kind: ConfigMap
metadata:
  name: jenkins-casc
  namespace: jenkins
data:
  jcasc.yaml: |
    # include contents of platform/apps/jenkins/jcasc.yaml here or reference directly by ArgoCD mount
    # (You can copy jcasc.yaml content into this key)