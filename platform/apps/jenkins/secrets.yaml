# ============================================================================
# FILE: platform/apps/jenkins/secrets.yaml
# PURPOSE: Example Jenkins secrets (DO NOT COMMIT ACTUAL SECRETS)
# NOTE: Use External Secrets Operator in production
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin
  namespace: fawkes
  labels:
    app.kubernetes.io/name: jenkins
    app.kubernetes.io/component: admin-credentials
  annotations:
    fawkes.io/note: "Example secret - use External Secrets in production"
type: Opaque
stringData:
  # For local development only
  # In production, use External Secrets Operator to fetch from:
  # - AWS Secrets Manager
  # - Azure Key Vault
  # - HashiCorp Vault
  ADMIN_USERNAME: admin
  ADMIN_PASSWORD: fawkesidp
