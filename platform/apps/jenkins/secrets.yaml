# ============================================================================
# FILE: platform/apps/jenkins/secrets.yaml
# PURPOSE: Example Jenkins secrets template (DO NOT COMMIT ACTUAL SECRETS)
# NOTE: This file shows the structure of required secrets
# USAGE: Create secrets manually or use External Secrets Operator in production
#
# Manual creation:
#   kubectl create secret generic jenkins-admin \
#     --from-literal=username=admin \
#     --from-literal=password=YOUR_SECURE_PASSWORD -n fawkes
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin
  namespace: fawkes
  labels:
    app.kubernetes.io/name: jenkins
    app.kubernetes.io/component: admin-credentials
    app.kubernetes.io/part-of: fawkes-platform
  annotations:
    fawkes.io/note: "Template only - create actual secret manually or via External Secrets"
type: Opaque
stringData:
  # PLACEHOLDER VALUES - Replace before applying
  # See README.md for instructions on creating secrets properly
  username: "CHANGEME_NOT_COMMITTED"
  password: "CHANGEME_NOT_COMMITTED"