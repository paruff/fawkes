# Minimal values for Jenkins Helm chart (only used if you deploy via Helm)
controller:
  adminUser: admin
  adminPassword: changeme
  JENKINS_OPTS: --argumentsRealm.passwd.admin=changeme
  serviceType: ClusterIP
persistence:
  enabled: false  # set true and configure storage for real workloads