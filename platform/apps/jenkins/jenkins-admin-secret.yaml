# ============================================================================
# FILE: platform/apps/jenkins/jenkins-admin-secret.yaml
# PURPOSE: Jenkins admin credentials secret (for local development)
# NOTE: In production, use External Secrets Operator instead
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin
  namespace: fawkes
  labels:
    app.kubernetes.io/name: jenkins
    app.kubernetes.io/component: admin-credentials
    app.kubernetes.io/part-of: fawkes-platform
  annotations:
    fawkes.io/managed-by: local-development
    fawkes.io/note: "For production, use External Secrets Operator"
type: Opaque
stringData:
  # Local development credentials
  # DO NOT USE IN PRODUCTION
  ADMIN_USERNAME: admin
  ADMIN_PASSWORD: fawkesidp
