apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin
  namespace: jenkins
type: Opaque
stringData:
  ADMIN_PASSWORD: changeme   # replace before production; better: use sealed secrets or external secret store