# ============================================================================
# FILE: platform/apps/jenkins/jenkins-admin-secret.yaml
# PURPOSE: Jenkins admin credentials secret template
# USAGE: This is a TEMPLATE - do NOT commit actual secrets!
#   For local development, create the secret manually:
#   kubectl create secret generic jenkins-admin \
#     --from-literal=username=admin \
#     --from-literal=password=YOUR_SECURE_PASSWORD -n fawkes
#
#   For production, use External Secrets Operator to sync from:
#   - AWS Secrets Manager
#   - Azure Key Vault
#   - HashiCorp Vault
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: jenkins-admin
  namespace: fawkes
  labels:
    app.kubernetes.io/name: jenkins
    app.kubernetes.io/component: admin-credentials
    app.kubernetes.io/part-of: fawkes-platform
  annotations:
    fawkes.io/managed-by: external-secrets
    fawkes.io/note: "Template only - use External Secrets Operator in production"
type: Opaque
stringData:
  # PLACEHOLDER VALUES - DO NOT USE IN PRODUCTION
  # Replace with actual values via kubectl create secret or External Secrets Operator
  username: "CHANGEME_NOT_COMMITTED"
  password: "CHANGEME_NOT_COMMITTED"