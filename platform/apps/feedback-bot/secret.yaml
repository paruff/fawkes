apiVersion: v1
kind: Secret
metadata:
  name: feedback-bot-secret
  namespace: fawkes
  labels:
    app: feedback-bot
    component: feedback
    platform: fawkes
type: Opaque
stringData:
  # API token for feedback service (if required)
  api-token: ""
  # Mattermost slash command token for validation (optional)
  bot-token: ""
