# ============================================================================
# FILE: platform/apps/eclipse-che/kustomization.yaml
# PURPOSE: Kustomize configuration for Eclipse Che resources
# ============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eclipse-che

resources:
  # Workspace resource quota for team namespaces
  - workspace-quota.yaml
  # Network policies for workspace isolation
  - network-policies.yaml

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/part-of: fawkes-platform
      app.kubernetes.io/component: cde
