# ============================================================================
# FILE: platform/apps/harbor/harbor-admin-credentials.yaml
# PURPOSE: Harbor admin credentials for initial login
# NOTE: For development/local use only. In production, use External Secrets
#       Operator with Vault/AWS Secrets Manager.
# SECURITY: Change these default passwords immediately after first deployment!
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: harbor-admin-credentials
  namespace: fawkes
  labels:
    app: harbor
    component: core
    environment: dev
    team: platform
type: Opaque
stringData:
  # Harbor admin password - CHANGE IMMEDIATELY IN PRODUCTION
  admin-password: "Harbor12345"
  # Secret key for encryption - GENERATE RANDOM KEY IN PRODUCTION
  secret-key: "not-a-secure-key-change-in-production"
