# ============================================================================
# FILE: platform/apps/harbor/kustomization.yaml
# PURPOSE: Kustomize configuration for Harbor resources
# ============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fawkes

resources:
  # Harbor admin credentials (dev/local)
  - harbor-admin-credentials.yaml
  # Note: For production, use External Secrets Operator
  # - externalsecret-harbor-admin.yaml

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/part-of: fawkes-platform
