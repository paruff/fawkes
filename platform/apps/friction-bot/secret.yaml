apiVersion: v1
kind: Secret
metadata:
  name: friction-bot-secret
  namespace: fawkes
  labels:
    app: friction-bot
    component: friction-logging
type: Opaque
stringData:
  # Replace with actual bot token from Slack/Mattermost
  # Or leave empty if token validation is not required
  bot-token: ""
