# ============================================================================
# FILE: platform/apps/hasura/serviceaccount.yaml
# PURPOSE: Service account for Hasura
# ============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hasura
  namespace: fawkes
  labels:
    app: hasura
    component: data-api
    environment: dev
