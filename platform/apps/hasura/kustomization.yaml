# ============================================================================
# FILE: platform/apps/hasura/kustomization.yaml
# PURPOSE: Kustomize configuration for Hasura deployment
# ============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fawkes

resources:
  - serviceaccount.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - redis.yaml
  - servicemonitor.yaml

commonLabels:
  app.kubernetes.io/name: hasura
  app.kubernetes.io/component: data-api
  app.kubernetes.io/part-of: fawkes
