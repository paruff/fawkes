# ============================================================================
# FILE: platform/apps/hasura/secret.yaml
# PURPOSE: Secret for Hasura admin credentials
# NOTE: In production, use External Secrets Operator or Sealed Secrets
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: hasura-admin-secret
  namespace: fawkes
  labels:
    app: hasura
    component: data-api
    environment: dev
type: Opaque
stringData:
  # Admin secret for Hasura console access
  # IMPORTANT: Change this in production!
  admin-secret: "fawkes-hasura-admin-secret-dev-changeme"
