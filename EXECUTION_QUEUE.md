# Fawkes — Execution Queue

> **Horizon:** Weeks | **Owner:** @paruff | **Review cadence:** Weekly, or whenever `plan-for-the-day.md`'s retrospective surfaces a backlog delta
> **Source:** Promoted from `docs/BACKLOG.md`'s triaged, value/effort-scored pool. This file is the single source of truth for *current* status (P0-P3, live-synced against GitHub) — `docs/BACKLOG.md`'s own status columns describe the same items at a coarser, less-frequently-updated grain and can lag behind this file. When the two disagree, trust this one and correct `docs/BACKLOG.md` to match, not the other way around.

## Priority Tiers

> Synced against live GitHub issue state 2026-09-17 (per the `audit-backlog-against-live-issue-state` technique). Re-verified every row against current GitHub state and repo contents rather than trusting prior claims.

> **2026-09-16→17:** Azure AKS cutover completed and re-verified end-to-end (`fawkes-dev-aks`, region migrated `eastus2`→`westeurope` same day, quota-clean rebuild). All 5 DORA keys are now live: 4 via native Prometheus recording rules (`platform/apps/prometheus/rules/dora.yml` — Deployment Frequency, Lead Time, Change Failure Rate, real Alertmanager-backed MTTR), the 5th (Rework Rate) resolved and implemented via GitHub PR data (`scripts/weekly-metrics.sh`, real baseline established in `docs/METRICS.md`). DevLake fully decommissioned, not just deprioritized. Jenkins→Tekton, Vault→OpenBao (planned), and this DORA-metrics pivot are now recorded in ADR-036/037/038 (`docs/adr/`), with ADR-004/015/016 marked superseded/in-progress accordingly. `mac-mini-k3s` remains deprioritized (root-caused to the `mini-gamer` WSL2 node specifically, not k3s or `lima-k3s-worker`) — see `plan-for-the-day.md`'s prior retro for detail, not re-litigated here. Cluster is destroyed nightly for cost (rebuild is fast — every fix from 2026-09-16 is in git; no rediscovery needed).

**P0 — blocks a release/phase gate.** Nothing else in this tier's phase can be honestly claimed done while these are open.

| # | Item | Why P0 | Status |
|---|---|---|---|
| — | **Live-verify Tekton's quality gate blocks a bad deploy** | Phase 1 of `docs/phase-2-closure-plan.md` — the "Security (core)" plane `MILESTONES.md` H2/Beta requires. See that file's Phase 1 checkbox for full detail: a real, previously-undiscovered bug (`sonar-scan` step permission-denied on every run, not just bad ones) was found and fixed first (`platform/apps/tekton/golden-path-pipeline.yaml`), then a real bad commit was proven to fail the quality gate and stop before `gitops-promote`, and a clean commit was proven to pass it and proceed. | **DONE 2026-09-17** on `fawkes-dev-aks` |
| #2117 | Wire real ArgoCD/Tekton/Alertmanager metrics into Prometheus | All acceptance criteria met live on `fawkes-dev-aks`: `argocd_app_sync_total{phase}`, `tekton_pipelines_controller_pipelinerun_duration_seconds_bucket`, and a real `ArgoCDAppDegraded` alert reaching Alertmanager's `/api/v2/alerts` (not a proxy). `dora.yml` rewritten to match real names, all 4 native-PromQL DORA keys verified through Grafana's actual query API. | **CLOSED** (2026-09-17) |
| — | ~~Decide + run the DevLake DB migration~~ | Moot — DevLake fully decommissioned 2026-09-16 (removed from `platform-applicationset.yaml`'s generator, live-pruned from cluster), not merely deprioritized. `devlake-mysql` PVC left untouched as a separate cleanup decision. | **RESOLVED BY DECOMMISSION** |
| — | ~~Azure AKS cutover~~ | Complete and re-verified after the West Europe region migration (2026-09-16 evening). ArgoCD bootstrapped, 12/14 platform Applications Synced/Healthy on last check, remainder (`cert-manager`, `tekton`) settling normally for a fresh install. | **DONE** |
| — | **Replace Jenkins with Tekton across all pipelines** | Migrate every Jenkinsfile/JCasC to Tekton pipeline/task definitions; update all CI references from Jenkins to Tekton; retire Jenkins infrastructure. Blocks all pipeline-dependent work. | **DONE** (2026-09-16) — Tekton pipeline templates for all 3 service skeletons; `services/samples/*/Jenkinsfile` deleted; `jenkins` Backstage Component and `jenkins.io/job-full-name` annotations removed from all 3 `catalog-info.yaml` files. Decision formally recorded in `docs/adr/ADR-036 tekton-for-ci-cd.md` (2026-09-16), superseding `ADR-004`. A stale local branch (`chore/remove-jenkins-integrations-and-docs`, unpushed) predates #2114 and deletes unrelated test files — not merged, not usable. |
| #1569 | Terraform remote state backend | `KL-01`: no state locking today — concurrent applies can corrupt state | **DONE** — Module created (`infra/terraform/modules/aws/terraform-state/`); terratest passes (not independently re-run this pass) |
| #1572 | Confirm DORA lead-time/deployment-frequency queryable in Grafana via native PromQL | Phase 1's last unverified acceptance criterion; replace DevLake-dependent queries with native PromQL | **DONE** (2026-09-17) — superseded by #2117's closure; both metrics confirmed live and queryable via all three Grafana query layers on `fawkes-dev-aks`. |
| #1855 | DevLake GitHub GraphQL collector | Root cause fixed (`KL-09`) but the issue itself is still open — re-verify and close once DevLake is reachable again (`KL-15`), don't just assume it's done | **CLOSED** (2026-09-16) — Root cause was missing token scopes (`read:user`, `user:email`); fix applied and verified live. DevLake now decommissioned entirely, not just optional. |
| #2004 | `extract-zip` arbitrary file write via symlinks (design-system build toolchain) | `p0-critical` security advisory, no patched version exists yet; related to the broader toolchain upgrade in #1715 | **ACCEPTED RISK** — Dev-only toolchain dep (`@lhci/cli` → Lighthouse CI → Puppeteer). No patched version exists (`extract-zip *` = all vulnerable). Documented as KL-20. Revisit when upstream migrates away from `extract-zip`. |
| — | **Grafana admin password hardcoded in plaintext in git** | `platform/apps/prometheus/prometheus-application.yaml:417`, `adminPassword: "fawkesidp"` — directly violates `AGENTS.md` §6 "Must Never: commit secrets". Found 2026-09-16 while answering a credentials question, not yet fixed. | **NOT STARTED** — needs OpenBao/External Secrets (blocked on ADR-037's implementation) or at minimum a Kubernetes Secret + `admin.existingSecret` in the interim |
| — | **Azure Terraform state backend (`fawkestfstate01`) was gone** | Found 2026-09-17 attempting a live progressive-delivery verification via `ignite.sh --provider azure`: the `fawkes-rg`/`fawkestfstate01` storage account documented in `infra/azure/backend.tf`'s header comment as "provisioned once, out-of-band" didn't exist. Recreated it via the exact commands in that file's comment; user granted `Storage Blob Data Contributor` RBAC (agents are classifier-blocked from granting RBAC, matches `AGENTS.md` §12); `fawkes-dev-rg`'s empty leftover shell was `terraform import`ed instead of deleted. | **RESOLVED 2026-09-17** — `ignite.sh --provider azure --cluster-name fawkes-dev-aks --location westeurope dev` completed clean end-to-end after the fix; `fawkes-dev-aks` provisioned, ArgoCD bootstrapped. |
| — | **Progressive delivery (canary) live-verified on Azure** | 2026-09-17: with the AKS cutover working again, live-verified `python-fawkes-path`'s canary + automated-rollback mechanism per `docs/phase-2-closure-plan.md` Phase 2 — see `docs/DEPLOYMENT_STRATEGY.md`'s Canary deployments row for full detail. Two forced Rollout revisions both paused at 50%, ran a Background `AnalysisRun`, and were auto-aborted/rolled-back with zero manual intervention. | **DONE** — gate-and-rollback mechanism proven live twice. New gap found in the process: `python-fawkes-path`'s `ServiceMonitor` never got picked up by `prometheus-operator` (see `docs/DEPLOYMENT_STRATEGY.md`), so the canary's *success* path (real passing metrics) is still unverified — worth its own follow-up before #1942 (chaos-in-canary) starts. |

**P1 — this sprint.**

| # | Item | Notes |
|---|---|---|
| #2116 | **Collapse 17 Python services into 2 domain monoliths** | **0% done in code.** `services/` on `main` still has all 18 original dirs; no `fawkes-telemetry-engine`/`fawkes-devex-service` exist on any branch. Local dirs with those names exist only as untracked, source-free `__pycache__`/`.venv` residue from 2026-09-15 — not real code. PR #2091 updated `docs/ARCHITECTURE.md` and `AGENTS.md` to describe the 2-monolith layout as current, which is aspirational, not actual — corrected in AGENTS.md 2026-09-16. Phased plan filed as #2116. |
| #1797 | Replace `CHANGE_ME_*` with Sealed Secrets | PR #2114 added the generation script + a validation test — but the test is designed to fail until the actual 15 placeholders are sealed, which hasn't happened. **Blocked on real cluster/kubeseal cert access** (not agent-executable). |
| #1578 | Wire IRSA role ARN into python-fawkes-path | PR #2114 renamed the Terraform module and wired the output, but the ARN still needs `terraform apply` + hardcoding into the live `python-fawkes-path-gitops` ServiceAccount. **Blocked — CI-gated `terraform apply` only, not run by hand per AGENTS.md.** |
| ~~#1581~~ | ~~Verify CI quality gates are green~~ | **CLOSED 2026-09-16** — verified per PR #2114. |
| #1938 | python-fawkes-path integration test suite | PR #2114 added 16 *structure* tests (file/dir layout), not the live-environment integration suite the issue title asks for. Partially done. |
| #1942 | Wire chaos experiments into canary traffic-shift | **Corrected 2026-09-17**: the previous "genuinely blocked, no Rollout exists" claim was sourced from a stale comment in `python-fawkes-path-beta-applicationset.yaml` — live-checked `python-fawkes-path-gitops`'s `deployment.yaml` and it's actually a real `Rollout` (canary: 50%→pause 60s→100%, `python-fawkes-path-canary-success` AnalysisTemplate wired), merged via python-fawkes-path-gitops#6 on 2026-09-12. Comment fixed. Not yet done: a live canary run on `fawkes-dev-aks` to confirm the analysis step actually gates promotion/rollback (blocked today on the Azure Terraform state backend RBAC issue below) — only once that's proven does #1942's chaos-in-canary wiring make sense to start. Design decision recorded for whenever it does: **on-demand trigger only**, not automatic on every canary. |
| #2032 | java-fawkes-path golden path (Phase 2/Beta) | PR #2114 added a design spec only; implementation lives in the separate `paruff/java-fawkes-path` repo and is estimated ~16h — too large for this queue's per-item granularity. Phased plan posted as a comment on #2032. Now also the designated pilot for #2120's new ApplicationSet pattern — don't deploy it via the old `path-based-applications` generator. |
| #2120 | Redesign golden-path ApplicationSets for multi-language, multi-environment scale | New 2026-09-16. Found while debugging why `python-fawkes-path` vanished: the shared `path-based-applications` ApplicationSet mixed platform infra with product golden paths and had a hardcoded, silently-stale file-path generator. Scoped: separate `golden-paths` ApplicationSet, auto-discovery generator, per-language+per-environment namespace isolation (`fawkes-<lang>-<env>` instead of shared `fawkes`). |
| — | **DevEx plane: Backstage + SPACE metrics** | New 2026-09-17. `services/space-metrics` already fully implements the SPACE framework (space-framework.com — Satisfaction/Performance/Activity/Communication/Efficiency), and `platform/apps/backstage` had complete manifests — neither was ever wired into an ApplicationSet's file generator, so neither deployed on a fresh cluster. Wired both in (PR pending), fixed space-metrics' stale `fawkes-local` namespace/local-only image/placeholder DB host, built+pushed `ghcr.io/paruff/fawkes-space-metrics`, and added `db-space-metrics-credentials` + a `space_metrics` role/database to `db-shared-dev-cluster.yaml`. **Live-verified on `fawkes-dev-aks`:** Backstage is genuinely running (pod healthy, API serving real 200s) even without Postgres/OAuth — it degrades gracefully rather than crashing. Catalog registration for space-metrics/devex-survey-automation was *already* correctly configured (`services/location.yaml`), but the catalog processor showed 0 indexed entities after 20+ min — not yet root-caused, may need a GitHub token for the integrations reader, separate from the OAuth login provider. **Status: RESOLVED 2026-09-17.** The `postgresql` bootstrap-ordering bug is fixed and live-verified on `fawkes-dev-aks`: split `cloudnativepg-operator-application.yaml` out into its own top-level Application (`platform-applicationset.yaml`, a real Helm chart, so its existing sync-wave `-5` now actually takes effect before postgresql's `-4`), and removed the 10 non-functional `secrets/*-sealed.yaml` entries (no sealed-secrets controller deployed, #1797). `db-shared-dev` now reports `Cluster in healthy state`. This surfaced two more real, separate bugs while live-verifying space-metrics against the now-working DB: (1) missing `greenlet` dependency for SQLAlchemy's async engine — fixed, rebuilt image, pod now `Running`/`Ready`, `/health` and `/metrics` serve real responses; (2) the SPACE collectors' own queries pass a timezone-aware datetime against `TIMESTAMP WITHOUT TIME ZONE` columns, erroring on every periodic collection cycle — **not fixed**, left as its own follow-up since it doesn't block startup/health. |
| #2124 | SRE practices — detection/alerting first, error budgets second | New 2026-09-16. Prompted by today's incident pattern: every failure found today (node outage, stuck ArgoCD controller, stale Helm release, stale ApplicationSet generator) was found by accident, hours to days late, not by any alert. Phase 1 (detection, unblocked now #2117 is closed) before Phase 2 (error budget definitions). **Found 2026-09-16:** `.agents/skills/runtime-operations/` already has generic `slo-management/error-budget`, `incident-detection`, `incident-triage`, `health-monitoring` skill templates (part of a pre-existing `uFawkesAI` skill suite, added in earlier commits, not this work) — none wired to a real Fawkes SLO/PromQL source yet. Reuse rather than rebuild when this issue starts. |
| #2126 | Build CI-gated terraform apply pipeline | New 2026-09-16. `AGENTS.md` says infra changes go through CI-gated apply; no such CI workflow exists — only plan/validate. Every apply today (Azure state backend, ArgoCD metrics, AKS cluster itself) was run by hand. Scope: PR-posted plan diff, human-approved apply in CI, apply-failure alerting. |

**P2 — next sprint.**

| # | Item | Depends on |
|---|---|---|
| #1919 | Verify DORA deployment-frequency/lead-time compute for tracer-bullet specifically | Unblocked (#2117 closed 2026-09-17) — repo-wide deployment frequency and lead time ARE live now (`dora:deployment_frequency:rate30d`, `dora:lead_time_hours:p50_30d`), but not yet confirmed scoped/filterable down to the `tracer-bullet` app specifically, which is this issue's actual ask. Ready to verify, not yet done. |
| #2079 | Alertmanager data into Prometheus for MTTR | **Mostly done, title is stale** — Alertmanager data already feeds MTTR (`dora:fdrt_hours:p50_30d` via `ALERTS_FOR_STATE`, real and live), not CFR as this row previously said. CFR (`dora:change_failure_rate:ratio30d`) comes from ArgoCD sync-failure ratio instead, independent of this issue. Remaining gap: MTTR only covers currently/recently-firing incidents, no historical p50 over 30d (Prometheus doesn't retain `ALERTS_FOR_STATE` post-resolution) — see `docs/adr/ADR-038`'s Consequences section. |
| #1946 | CFR panel on DORA dashboard | **Closer than previously scoped** — CFR data already exists and is live (`dora:change_failure_rate:ratio30d`, independent of #2079/MTTR). Just needs a Grafana panel wired to it; not blocked on Alertmanager work as this row previously implied. |
| #2081 | Phase 5 self-measurement MVP (fawkes-on-fawkes DORA) | Unblocked (#2117 closed 2026-09-17) — the underlying metrics now compute for the fawkes repo itself via ArgoCD/Tekton, which is what this issue asks for; not yet independently checked against #2081's own specific acceptance criteria. |

**P3 — backlog.**

| # | Item |
|---|---|
| #1156 | Wire `gen_ai.*` OTEL spans to Prometheus |
| #1922 | Move local k8s dev off the MacBook onto a multi-node pool |
| — | GCP support (explicit `VISION.md` non-goal for this stage — don't pull forward without a vision-level decision) |

## Scope-Drift Protection

Before adding anything to this queue, check it against `VISION.md`'s Non-Goals for the Current Stage. If a proposed item is really Backstage-portal work, RBAC/production-gating work, or GCP support — it belongs in `MILESTONES.md`'s H2/H3 rows, not here. Adding it to P1/P2 "because it's a quick win" is exactly the scope creep the vision tier exists to catch.

## Bottom-Up Feedback

`plan-for-the-day.md`'s retrospective section is the input side of this file, not just an output. When a day's work surfaces something this queue didn't anticipate — a new blocker, a task that was mis-sized, a P2 that turned out to actually be P0 — that correction is recorded in the retro and then folded back into this file's tiers before the next planning pass. This queue should never silently drift from what the daily retros are actually finding.

## How This Connects

| Tier | File | What it answers |
|---|---|---|
| ↑ Months | [MILESTONES.md](MILESTONES.md) | Which phase is this work in service of, and what's the release gate? |
| ↑ Source pool | [docs/BACKLOG.md](docs/BACKLOG.md) | Where did this item come from, and what's its value/effort score? This file is downstream of it for status — see the note at the top. |
| ↓ Today | [plan-for-the-day.md](plan-for-the-day.md) | What's the one thing being worked on right now? |
